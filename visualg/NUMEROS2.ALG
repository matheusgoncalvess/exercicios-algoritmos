Algoritmo "numeros"

Var

   num1, num2, h, area : real

Inicio

      num1 <- 6.0
      num2 <- 8.0
      h <- 5.0
      
      area <- (num1 + num2) / 2.0 * h)
      
      escreval(area)

Fimalgoritmo